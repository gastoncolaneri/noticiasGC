import { makeStyles } from "@mui/styles";

export const generalStyles = makeStyles((theme) => ({
  logo: {
    marginRight: 10,
    alignSelf: "center",
    alignItems: "center",
    alignContent: "center",
  },
}));
